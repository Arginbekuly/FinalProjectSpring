server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/theory
    username: theories
    password: secret

services:
  user:
    url: ${USER_SERVICE_URL:http://host.docker.internal:8081}
